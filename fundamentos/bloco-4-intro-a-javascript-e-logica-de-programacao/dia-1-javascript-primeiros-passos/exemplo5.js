let weekDay = "quarta";

if(weekDay === "segunda" || weekDay === "terça" || weekDay === "quarta" || weekDay === "quinta"){
    console.log("Oba, dia letivo na Trybe");
}
else{
    console.log("obaaa, fim de semana!");
}